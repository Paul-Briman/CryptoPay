[phases]
setup = "nodejs_18, npm-9_x, openssl"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run railway:build"]

[phases.start]
cmds = ["npm run railway:start"]

[static]
path = "dist/client"
spa = true