{
  "version": 2,
  "builds": [
    {
      "src": "package.json",  
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/client",
         "buildCommand": "npm run vercel-build"  
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)", 
      "dest": "dist/client/$1"
    }
  ]
}